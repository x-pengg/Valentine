<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.ridog.valentine.mapper.IMetasMapper">

    <select id="selectAll" resultMap="me.ridog.valentine.mapper.auto.MetasMapper.BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="me.ridog.valentine.mapper.auto.MetasMapper.Base_Column_List"/>
        FROM valentine_metas WHERE type = #{type}
    </select>

</mapper>